import {
  ActionWithRulesAndAddersProps,
  ActionElement,
} from "react-querybuilder";

export const CustomAddRule = (props: ActionWithRulesAndAddersProps) => {
  return <ActionElement {...props} />;
};
